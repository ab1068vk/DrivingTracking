/** Fixture page for the transitive consumer-oracle mutation test. */
import { fixtureEntryHelper } from './helpers';

export const renderFixturePage = (summaries = []) => fixtureEntryHelper(summaries);
